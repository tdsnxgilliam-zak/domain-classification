"""Worldwide Domain Inventory Analysis Pipeline.

A pure-Python heuristic pipeline that classifies every domain in the source
workbook (``url-regional-class.xlsx``) and produces a deliverable CSV + xlsx.

See ``docs/spec.md`` for the full specification and ``docs/tasks.md`` for the
atomic build steps.
"""

__version__ = "1.0.0"
